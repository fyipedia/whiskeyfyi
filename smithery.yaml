# Smithery configuration for whiskeyfyi
# https://smithery.ai/docs/build/project-config
startCommand:
  type: stdio
  configSchema:
    type: object
    required: []
    properties: {}
  commandFunction: |-
    config => ({ command: 'uvx', args: ['--from', 'whiskeyfyi[mcp]', 'python', '-m', 'whiskeyfyi.mcp_server'] })
